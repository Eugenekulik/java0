package by.training.task5.controller.command;

/**
 * enum with command's names.
 */
public enum CommandType {
    /**
     * Exit command.
     */
    EXIT,
    /**
     * Command which do changes with matrix's diagonal.
     */
    MATRIXCHANGE
}
